<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark only (design doc §8.1); Compose draws everything, edge to edge.
         AppCompat parent: BiometricPrompt's fingerprint dialog (API < 28) needs it. -->
    <style name="Theme.ObliNext" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/app_window_background</item>
        <item name="android:colorBackground">@color/app_window_background</item>
    </style>
</resources>
