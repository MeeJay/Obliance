node_modules/
dist/
!agent/dist/
agent/dist/*.exe
agent/dist/*.wixpdb
desktop-app/dist/*.wixpdb
agent/bin/*.exe
agent/*.exe
!desktop-app/dist/
!oblireach-desktop/dist/
oblireach-desktop/dist/*.wixpdb
.env
*.log
.DS_Store
Thumbs.db
coverage/
.vscode/
.idea/
*.tsbuildinfo
*.bat
000-RegularUpdate.lock

# Claude Code internal files (conversation history, memory, etc.)
.claude/
CLAUDE.md

# Local dev / planning notes
PLAN.md
custom/
start.ps1
# Build-time //go:embed payloads that the wizard sources copy in before
# compilation and remove after. Should never be committed — the canonical
# binaries live in agent/dist/.
agent/cmd/wizard/obliance-agent.msi
agent/cmd/wizard-linux/obliance-agent
